'use strict';

/**
 * onboarding-step controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::onboarding-step.onboarding-step');
